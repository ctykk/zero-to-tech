# Zero to Tech - 前端

前端基于 **React 19 + TypeScript + Vite** 构建，包管理使用 Bun。

## 启动

```bash
bun install      # 安装依赖
bun run dev      # 启动开发服务器 (Vite)
bun run build    # 生产构建
bun run preview  # 预览生产构建
```
